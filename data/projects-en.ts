import { ProjectStory } from "@/lib/types";
import { projects } from "./projects";

// For now, English uses the same data as Japanese.
// Replace with translated content when ready.
export const projectsEn: ProjectStory[] = projects;
